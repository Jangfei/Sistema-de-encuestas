  <%= simple_form_for(@object, :url => collection_url, :remote => is_xhr?) do |f| %>
	    <%= render :partial => "form", :locals => { :f => f } %>
		<%= f.button :submit %>
		<%= cancel_button_tag(collection_url)%>
  <% end %>
